POSTGRES_DB=chronicle
POSTGRES_USER=chronicle
POSTGRES_PASSWORD=change-me
CHRONICLE_JWT_SECRET=change-me-too
CHRONICLE_SYNC_TOKEN=change-sync-token
CHRONICLE_CORS_ORIGIN=http://localhost:8080
MEILI_MASTER_KEY=change-meili-key
MINIO_ROOT_USER=chronicle
MINIO_ROOT_PASSWORD=change-minio-password

# Redis Configuration (required for AUTH-102)
# Used for refresh token storage - enables horizontal scaling
# Format: redis://host:port/db or redis://user:pass@host:port/db
REDIS_URL=redis://localhost:6379/0

# SMTP Configuration for email notifications (optional)
# If not configured, email verification and password reset emails won't be sent
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@example.com
SMTP_FROM_NAME=Chronicle
